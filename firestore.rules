rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal access for full app functionality across all collections and subcollections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}


